## What this changes

<!-- What it does and why. If it fixes an issue, link it. -->

## How it was tested

<!-- `python3 -m unittest discover -s tests` is the minimum. If it touches the
     network paths, say what you ran it against and what came back. -->

## Checklist

- [ ] Standard library only. No new import from outside the stdlib.
- [ ] No number is reported without its spread, and lab and field figures stay
      visibly separate.
- [ ] Tests pass on Python 3.9 as well as current.
- [ ] Nothing writes an API key to disk outside the user's config directory, or
      prints one to stdout, a log, or a tool result.
